--- AppViewModelTest.kt
+++ AppViewModelTest.kt
@@ -183,9 +183,7 @@
             ShadowLooper.runUiThreadTasksIncludingDelayedTasks()
             attempts++
         }
-        if (viewModel.discMetadata.value?.albumTitle != "Unknown Album") {
-            assertNull(viewModel.discMetadata.value)
-        }
+        assertNull(viewModel.discMetadata.value)

         job.cancel()
         job.join()
